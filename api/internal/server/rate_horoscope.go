package server

import "net/http"

func (s *Server) RateHoroscope(w http.ResponseWriter, r *http.Request) {
	// TODO Get horoscope by external id and rating = null
	// check if it exists

	// rating is one of the valid statuses
	// update rating

}
